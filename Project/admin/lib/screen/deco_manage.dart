import 'package:flutter/material.dart';

class DecoManage extends StatelessWidget {
  const DecoManage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Decoration"),
    );
  }
}